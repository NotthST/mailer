for p in "${patterns[@]}"; do echo "Searching for: $p"; find . -type f -name "*.gz" ! -name "*.tar.gz" -exec zgrep -in --color=always "$p" {} +; find . -type f -name "*.tar.gz" -exec sh -c 'tar -xOzf "$1" | grep -in --color=always "$2"' _ {} "$p" \; ; find . -type f -name "*.zip" -exec zipgrep -in --color=always "$p" {} +; echo "--------------------------------------"; done > /Home/
