for p in "${patterns[@]}"; do echo "Searching for: $p"; find . -type f -name "*.gz" -exec zgrep -in --color=always "$p" {} + || echo "No matches for $p"; echo "--------------------------------------"; done > /Home/
