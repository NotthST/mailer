for p in "${patterns[@]}"; do echo "Searching for: $p"; find . -type f \( -name "*.gz" ! -name "*.tar.gz" -exec zgrep -in --color=always "$p" {} + -o -name "*.tar.gz" -exec sh -c 'tar -xOzf "{}" | grep -in --color=always "$1"' _ "$p" \; -o -name "*.zip" -exec zipgrep -in --color=always "$p" {} + \) || echo "No matches for $p"; echo "--------------------------------------"; done > /Home
