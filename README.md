for p in "${patterns[@]}"; do echo "Searching for: $p"; find . -type f -name "*.zip" -exec zipgrep -in --color=always -e "$p" {} + || echo "No matches in .zip for $p"; find . -type f -name "*.tar.gz" -exec sh -c 'tar -xOzf "$1" | grep -in --color=always -e "$2"' _ {} "$p" \; || echo "No matches in .tar.gz for $p"; find . -type f -name "*.gz" ! -name "*.tar.gz" -exec zgrep -in --color=always -e "$p" {} + || echo "No matches in .gz for $p"; echo "--------------------------------------"; done > /Home/
