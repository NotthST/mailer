for p in $patterns; do echo "Searching for: $p"; if ! find . -type f -name "*.zip" -exec zipgrep -in --color=always -e "$p" {} + ; then echo "No matches in .zip for $p"; fi; if ! find . -type f -name "*.tar.gz" -exec sh -c 'tar -xOzf "$1" | grep -in --color=always -e "$2"' _ {} "$p" \; ; then echo "No matches in .tar.gz for $p"; fi; if ! find . -type f -name "*.gz" ! -name "*.tar.gz" -exec zgrep -in --color=always -e "$p" {} + ; then echo "No matches in .gz for $p"; fi; echo "--------------------------------------"; done > /Home/kf
