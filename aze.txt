patterns=("password" "api_key" "secret_token" "confidential"); for p in "${patterns[@]}"; do echo "🔍 Searching for: '$p'"; grep -RInI --color=always "$p" . || echo "No matches for '$p'"; echo "------------------------------------"; done
