# Force Excel to show the raw values EXACTLY as they appear
def excel_text(v):
    if v == "":
        return ""
    return f'="{v}"'   # Excel-safe text literal

output_rows.append([
    citmid,
    status,
    excel_text(raw1),
    excel_text(raw2)
])
